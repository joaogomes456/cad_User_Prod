import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'io.ionic.starter',
  appName: 'cad_User_Prod',
  webDir: 'www',
  bundledWebRuntime: false
};

export default config;
